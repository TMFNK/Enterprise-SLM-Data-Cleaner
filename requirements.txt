# runtime + tooling
requests>=2.31        # talk to the llama.cpp OpenAI-compatible server
pyyaml>=6.0           # load the convention spec (conventions/*.yaml)

# optional: semantic alias resolution (bge-m3 with sentence-transformers, ~2.2 GB download)
#   pip install sentence-transformers>=3.0
# activate via: USE_EMBEDDINGS=1

# fine-tuning (Apple Silicon) — install separately if training:
#   pip install mlx-lm
# inference: build or `brew install llama.cpp`
